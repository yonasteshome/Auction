# SpendSense in Auction

This folder contains a helper script to copy the existing `SpendSense` workspace into this `Auction` folder as a subfolder.

Usage (from Windows `cmd`):

```
cd Auction
move_spendsense.cmd
```

After running, the `SpendSense` files will be available at `Auction/SpendSense`.
